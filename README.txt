 Lab 4 – Student Registration (WAD621S)

A minimal, accessible registration page:
- Labels + fieldsets, inline validation, 'aria-live' feedback
- Profile cards with programme + "Year + Interests" row
- Summary table kept in sync; Remove deletes both
- Mobile friendly, no big white gaps (tight grid)

 Run
Open 'index.html' in any browser.

 Files
- index.html, style.css, script.js, README.md

 Dev Notes
- Custom JS validation replaces default popups (clear inline messages).
- Shared 'data-id' links cards/table for remove.

- Cards grid uses `repeat(auto-fill,minmax(...))` to pack without gaps.


